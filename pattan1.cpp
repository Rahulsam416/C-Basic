#include<iostream>
using namespace std;

int main(){
    int row,col;
     
    cout << "Enter the rows nunber: ";
    cin >> row;

    cout << "Enter the coloms number";
    cin >> col;

    {
        for (int j = 1; j <= col; j++)
        {
    for (int i = row; i >= 1; i--)
    for (int i = row; i >= 1; i--)
    
    for (int i = row; i >= 1; i--)
            cout << i << " ";
        }
        cout << endl;
    }
    
    return 0;
}